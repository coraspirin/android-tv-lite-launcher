<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/veil">

    <LinearLayout
        android:layout_width="@dimen/panel_width"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/menu_bg"
        android:orientation="vertical"
        android:padding="28dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:text="@string/mirror_title"
            android:textColor="@color/ink_on_glass"
            android:textSize="22sp" />

        <TextView
            android:id="@+id/mirror_state"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center"
            android:textSize="15sp" />

        <Switch
            android:id="@+id/mirror_toggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="24dp"
            android:background="@drawable/chip_bg"
            android:focusable="true"
            android:fontFamily="sans-serif-medium"
            android:paddingStart="@dimen/chip_h_padding"
            android:paddingTop="@dimen/chip_v_padding"
            android:paddingEnd="@dimen/chip_h_padding"
            android:paddingBottom="@dimen/chip_v_padding"
            android:contentDescription="@string/mirror_toggle"
            android:thumbTint="@color/switch_thumb"
            android:trackTint="@color/switch_track"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
